# TODO.md

Список задач проекта WhiteListCheck. Статусы: ✅ сделано · 🔄 в работе ·
⬜ не начато · 💤 отложено.

## Выпущено

**v0.2.1 (08.07.2026):** release-подпись из CI; контрольные сайты
группы Б заменены (Drive2, Banki.ru → Google, Википедия); README обновлён.

**v0.3 (10.07.2026):** экран настройки списков (SharedPreferences,
диалоги подтверждения, сброс); фоновая проверка использует
пользовательские списки; автоименование APK (шаг Rename в build.yml;
archivesBaseName в Gradle не работает — не повторять).

**v0.3.1 (11.07.2026):** Android TV, этап 1 — баннер, LEANBACK_LAUNCHER,
uses-feature required=false.

**v0.3.2 (12.07.2026):** Android TV, этап 2 — подсветка фокуса D-pad
(tvFocusHighlight; onFocusChanged ДО clickable).

**v0.3.3 (12.07.2026):** Android TV, этап 3 — TRANSPORT_ETHERNET,
«Ethernet (кабель)» + пояснение. ТВ-поддержка базово полная.

**v0.4 (14.07.2026), редизайн:** светлая + тёмная темы по системе;
Golos Text (res/font, OFL); закреплённые шапка и вердикт + единая
прокрутка; чип «Сеть: … ⓘ»; сворачиваемые карточки групп; значки
статуса Check/Close (Cancel в базовом наборе нет, extended
не подключать); расшифровка ошибок за ⓘ. versionCode 8.

**v0.4.1 (19.07.2026):** кнопка «поделиться вердиктом» (справа от чипа
сети, Icons.Outlined.Share + offset −1dp, текст со ссылкой на RuStore);
ТВ всегда в тёмной теме (isAppDark: FEATURE_LEANBACK || система; на неё
переведены ВСЕ цветовые помощники, не только AppTheme); подсказка при
VPN (TRANSPORT_VPN проверяется ПЕРВЫМ — с Android 12 VPN-сеть сообщает
и нижележащий транспорт; чип «Сеть: VPN ⓘ» с пояснением, без
сканирования приложений и GeoIP); чип сети скрыт во время проверки;
выравнивания offset-сдвигами (шестерёнка +12dp, стрелка назад −12dp,
«Почему ошибки разные» −4dp); копирайт выше ссылки версии (ТВ-прокрутка
следует за фокусом); из main: ProbeRow weight+textAlign.End, TLS
в сноске. versionCode 9.

## Релиз v0.4.2 — сохранение состояния

Правки в main (MainActivity.kt + AndroidManifest.xml):
- ✅ rememberSaveable вместо remember в трёх раскрывашках (чип сети,
  карточки групп, «Почему ошибки разные») — раскрытое состояние
  переживает прокрутку за экран (LazyColumn уничтожает уехавшие
  элементы вместе с remember) и пересоздание экрана
- ✅ AndroidManifest: android:configChanges="uiMode|orientation|screenSize"
  у activity — смена светлой/тёмной темы и поворот экрана больше
  не пересоздают Activity и не сбрасывают результаты проверки
  (Compose перекрашивается и перестраивается сам)

Оформление релиза:
- ⬜ Поднять версию в app/build.gradle.kts (versionCode 10, "0.4.2"),
  дождаться зелёной сборки
- ⬜ Смоук-тест APK: смена темы при открытых результатах (остаются
  и перекрашиваются), прокрутка с раскрытым ⓘ (остаётся раскрытым),
  поворот экрана (результаты на месте)
- ⬜ GitHub Release: тег v0.4.2, приложить APK, Set as latest
- ⬜ Загрузить v0.4.2 обновлением в RuStore
- ⬜ Закоммитить обновлённые TODO.md и STATE.md

## Дальше — кандидаты в v0.5

- ⬜ **Локальный лог проверок с выгрузкой.** Записывать итог каждой
  проверки (ручной и фоновой) строкой вида
  «19.07.2026 08:15 [фон] сеть=мобильный вердикт=БЕЛЫЙ СПИСОК
  A=4/4 B=0/4 C=0/3» в файл во внутренней папке приложения;
  файл на день (netlog-ГГГГ-ММ-ДД.txt), при записи удалять файлы
  старше 3 дней. Объём: ~50 КБ/сутки при фоне раз в 15 мин —
  памяти не ест, батарею не трогает (пишется итог уже выполненной
  проверки). UI: тумблер вкл/выкл и кнопка «выгрузить лог» на экране
  настроек (через системное «Поделиться»). Обновить PRIVACY.md:
  лог хранится локально, отправляется только вручную самим
  пользователем. Польза: отчёты из регионов с временными метками
  (когда белый список включили/выключили), диагностика убийства
  фонового воркера MIUI (дырки в логе), помощь в разборе UNKNOWN
- ⬜ **Доработать вердикт UNKNOWN «непонятная ситуация».** Возникает,
  когда группа А (белый список) недоступна, а группа Б работает.
  Известные причины:
  (а) пользователь за границей или через VPN, а в группе А остались
  только сайты, не открывающиеся с зарубежных IP (например, одни
  Госуслуги после ручной чистки списка);
  (б) сбой/недоступность российских сайтов при работающих зарубежных.
  Придумать: более понятный текст вердикта, возможно отдельный вердикт
  «российские сайты недоступны, остальное работает», и совет держать
  в группе А несколько РАЗНЫХ сайтов (подсказка при редактировании
  списка, если в группе остался 1 сайт?)
- ⬜ Собрать обратную связь по кнопке «поделиться» (4PDA, знакомые)

## Магазины

- 🔄 RuStore: v0.4 опубликована; загрузить актуальную версию
  обновлением (сразу v0.4.2, чтобы не гонять модерацию дважды)
- 🔄 RuMarket, GetApps: подана v0.3.1 (~11.07) — проверить статусы,
  при одобрении обновить до актуальной версии
- ✅ Ссылка на карточку RuStore и кнопки установки добавлены в README
- ⬜ Обновить скриншоты в магазинах на актуальный дизайн (старые — v0.3.x)
- ⬜ Категория Android TV в RuStore (после публикации)

## Продвижение

- 🔄 4PDA: тема создана 17.07.2026, на премодерации. Следить за QMS.
  После публикации: написать модератору о статусе автора темы,
  отвечать в теме, собирать отчёты из регионов
- ⬜ Письмо блогеру VladimirKoPSS (черновик согласован в чате)
- ⬜ Пост в Telegram-каналы
- ⬜ Попросить пользователей из других регионов проверить актуальную
  версию

## Известные мелочи (не блокируют)

- 💤 ТВ: кнопка сброса списков прилипает к нижнему краю экрана
  (прокрутка следует за фокусом и не довозит отступ под кнопкой);
  чистого решения без экспериментальных API нет
- 💤 VPN на роутере не определяется чипом сети — системное ограничение
  Android, вердикт при этом всё равно отработает по доступности сайтов

## Долгосрочные идеи

- 💤 Удалённое обновление списков (REMOTE_CONFIG_URL, хостинг
  из белого списка)
- 💤 Кириллические домены в настройках (punycode)
- 💤 Ручной переключатель темы (по запросу пользователей)
- 💤 Переименовать APK в старых релизах v0.1–v0.2.1
- 💤 Google Play: $25, зарубежная карта, 12 тестировщиков × 14 дней, AAB
- 💤 Верификация Android-разработчиков Google (~2027), бесплатный
  уровень на 20 устройств
